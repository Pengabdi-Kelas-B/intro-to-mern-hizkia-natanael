{
  "name": "backend",
  "version": "1.0.0",
  "description": "Mock server untuk Pokedex Web menggunakan json-server",
  "main": "generateJsonDB.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:db": "node generateJsonDB.js",
    "prestart": "npm run generate:db",
    "start": "json-server --watch db.json --port 3001"
  },
  "keywords": ["pokedex", "mock-server", "json-server"],
  "author": "Natan",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "json-server": "^0.17.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/username/repo.git"
  }
}
